import DirectionTable from '../components/Directions/DirectionTable.jsx';

const Directions = () => {
    return (
        <div>
            <DirectionTable />
        </div>
    );
};

export default Directions;